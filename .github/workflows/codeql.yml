name: iOS CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  analyze:
    name: Analyze (Swift)
    runs-on: macos-latest

    steps:
      - name: Checkout repository
      - run: echo "No errors found"
