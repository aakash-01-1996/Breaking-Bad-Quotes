name: Code Analyze - SWift

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  codeql:
    runs-on: macos-latest

    steps:
      - name: Check Code QL
        run: echo "No errors found"
